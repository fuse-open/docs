language: csharp
mono: none
dotnet: 2.1.102

script:
  - ./build.bash

addons:
  apt:
    packages:
      - imagemagick
      - pngquant
      - optipng

deploy:
  provider: pages
  local-dir: _site
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
